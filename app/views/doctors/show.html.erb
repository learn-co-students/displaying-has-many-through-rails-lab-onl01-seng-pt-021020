<% @doctor.appointments.each do |appointment| %>
    <h3><%= appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M")  %></h3>
    <h1> <%= link_to appointment.patient.name, patient_path(appointment.patient) %></h1>
<% end %>