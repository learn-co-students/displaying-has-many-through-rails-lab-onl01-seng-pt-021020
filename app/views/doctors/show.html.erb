<h1>Doctor: <%= @doctor.name %></h1>
<h2> <%= @doctor.department %></h2>
<div>
  <h2>Appointments:</h2>
  <% @doctor.appointments.each do |a| %>
    <p>Date/Time: <%= a.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></p>
    <p>Patient: <%= link_to a.patient.name, patient_path(a.patient) %></p>
  <% end %>
</div>